module.exports = {
  title: '{{ config.title }}',
  description: '{{ config.description }}',
  themeConfig: {
    sidebar: [
      {% for group in config.groups %}
      '/generated/{{ group.toFileName }}',
      {% endfor %}
    ],
    displayAllHeaders: true,
    smoothScroll: true,
  },
}
